### 概要
2つの整数 `a` と `b` が与えられます。2つの数の和を返す関数 `add_two_numbers(a, b)` を実装してください。

### 制約
* $-10^9 \le a, b \le 10^9$

### 入出力例
* **例 1:** `a = 3, b = 5` $\rightarrow$ `8`
* **例 2:** `a = -10, b = 4` $\rightarrow$ `-6`
* **例 2:** `a = -6, b = -9` $\rightarrow$ `-15`